using UnityEngine;

public class Cell : MonoBehaviour
{
    public int gridX; // The x-coordinate of the cell in the grid
    public int gridY; // The y-coordinate of the cell in the grid
    public bool isOccupied = false; // Whether the cell is occupied by a tile

    // Start is called once before the first execution of Update after the MonoBehaviour is created
    void Start()
    {

    }

    // Update is called once per frame
    void Update()
    {

    }
}
